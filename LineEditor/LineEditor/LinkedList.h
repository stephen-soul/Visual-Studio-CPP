#pragma once

// Includes
#include <iostream>
#include <string>
#include <sstream>
#include <fstream>

// Class
class LinkedList
{
public:
	// Constructor & Destructor

	// Functions
	bool tryToFindFile(std::string fileName, std::string newFileName);
	void openFileIntoList(std::string fileName, std::string newFileName);
	
	// Variables
};

